import { Box } from "@mui/material";
import React from "react";

export const ProgressSlider = () => {
	return (
		<Box
			id="progress-bar-controller"
			sx={{ height: 3.5, width: "100%", background: "rgba(0,0,0,0.3)" }}
		/>
	);
};
